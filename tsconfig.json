{
	"extends": "@deslop/workflow/tsconfig.json",

	"compilerOptions": {
		"jsx": "react-jsx",
		"lib": ["DOM", "ESNext", "DOM.Iterable"],
		"types": ["node", "vite-plus/client"]
	},

	"exclude": [
		"${configDir}/**/node_modules",
		"${configDir}/**/dist",
		"${configDir}/**/.output",
		"${configDir}/tools/*/template/**/*"
	]
}
