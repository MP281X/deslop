---
name: workflow
description: 'Use when changing the reusable agent workflow: the pair-thread text, roles, Codex and Claude Code configuration, or installer. This is the durable handoff; do not reanalyze old threads for facts recorded here.'
---

# Workflow handoff

## Current state

| Layer      | State on 2026-09-24                                                                                                                                                            |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Source     | Pair text in `claude/output-styles/pair.md` and the Codex `developer_instructions`, identical. Roles explore, implement, browser, git, general, review, identical per harness. |
| Installed  | Installed live from branch `t3code/improve-agent-workflow` (PR #82); new sessions load it.                                                                                     |
| Skills     | `.claude/skills` symlinks `.agents/skills`, so Claude sees `project-engineering` and `workflow`; verified with a probe session.                                                |
| Unverified | No real thread has run the 2026-09-24 version: review role, expensive steps once per delivery, proportional follow-ups, proactive prototypes, vp/vpx only.                     |

Next useful evidence, from a real task in a fresh session:

1. The pair executes nothing itself beyond one-line fixes; slices run in parallel.
2. Substantial work shapes an MVP, cut list, acceptance checks, and batched decisions, then executes without check-ins.
3. The first "done" is ready to merge: no user cleanup, reverts, or status prompts.

## Contract

| Message                       | Pair behavior                                                                                                                                           |
| ----------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Question or problem           | Assessment with evidence, root cause, recommendation; no changes                                                                                        |
| Small change request          | Delegated slice, local check, ready-to-merge review, commit, push, draft request                                                                        |
| Substantial work: shaping     | Interactive. Ground existing contracts, then MVP, cut list, per-task acceptance checks, and every open decision as one batch of questions               |
| Substantial work: execution   | Autonomous after approval. Disjoint-file slices in parallel, hard slices on the strongest model per call, acceptance verified by the pair, no check-ins |
| Steer while work is in flight | Answered or queued; in-flight work continues                                                                                                            |

Standing rules: the pair only coordinates; a ledger carries the user's decisions into every brief and is checked against merges; mechanics happen without asking, decisions are discussed; the engineering skill's Simplicity section bounds scope; checks, lint, and review are fallbacks; done means ready to merge with the thread's artifacts removed; output is schematic.

## Ownership

| Owner     | Owns                                                                                                    | Never                                                              |
| --------- | ------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------ |
| Pair      | outcome, shaping, ledger, briefs, synthesis, acceptance verification through agents                     | execution beyond a one-line fix; the default branch                |
| Explore   | one bounded evidence source; located facts                                                              | edit, recommend, measure, overlap another source                   |
| Implement | one slice at high effort, engineering skill preloaded, scoped proof, an assigned merge left uncommitted | git delivery, scope decisions, behavior-changing mechanical passes |
| Browser   | rendered criteria and observed defects or pass, once per delivery, launched by the pair                 | implementation                                                     |
| Git       | commit messages derived from the diff, push, draft request body refreshed on every push                 | branches, merges, the default branch, rerunning proof              |
| General   | delegated work that fits no other role: environment, evals, research                                    | product code, git, rendered checks                                 |
| Review    | one adversarial pass over a finished branch diff, every finding at once                                 | edits, runs, a second round                                        |
| t3        | worktrees, branches, diff review, PR linking                                                            | workflow decisions                                                 |

## Source and delivery

The pair and each role are duplicated once per harness; edit mirrored bodies identically. There is no renderer.

| Source                                                 | Installed destination / purpose                                              |
| ------------------------------------------------------ | ---------------------------------------------------------------------------- |
| tools/workflow/src/agents/codex/config.toml            | ~/.codex/config.toml; Codex pair text, features, role registrations          |
| tools/workflow/src/agents/codex/AGENTS.md              | ~/.codex/AGENTS.md; authorizes configured roles                              |
| tools/workflow/src/agents/codex/agents/*.toml          | Codex role bodies, models, efforts                                           |
| tools/workflow/src/agents/claude/output-styles/pair.md | Claude pair text as output style `pair`                                      |
| tools/workflow/src/agents/claude/settings.json         | Claude settings: `outputStyle`, denied built-ins, default-branch deny rules  |
| tools/workflow/src/agents/claude/agents/*.md           | Claude role bodies, models, efforts, background flags                        |
| tools/workflow/src/agents/skills/engineering/SKILL.md  | shared product-code conventions copied to both homes                         |
| tools/workflow/src/install.ts                          | replaces managed top-level entries; removes the obsolete ~/.claude/CLAUDE.md |

Build in tools/workflow with `vp run build`, then run `node dist/install.js`. Install after every workflow change without asking. `CODEX_HOME` and `CLAUDE_CONFIG_DIR` select scratch destinations. A fresh session loads the result.

Harness facts, verified 2026-09-22:

- t3 launches Claude with `--setting-sources=user,project,local` and no system-prompt override.
- A user-level output style reaches the Claude main thread only. A subagent without `omitClaudeMd` receives the repository AGENTS.md; with it, nothing. Roles therefore omit it.
- Claude loads project skills only from `.claude/skills`. `Monitor` is allowed for waits. Deny rules block pushes to main or master and `gh`/`glab` merges by prefix; a bare `git push` with upstream main is not caught.
- Codex roles' developer instructions replace the root's. `[agents] max_depth` is undocumented and was removed; `goals` stays off. Codex role files honor only developer_instructions, model, reasoning effort and summary, verbosity, personality, and service tier; under the V2 agent tools no config stops a spawned role from spawning, so roles state it and the pair enforces it. Running Claude sessions pick up reinstalled agent definitions: a thread's agents switched from Sonnet to Opus after the 10:13 install. Codex command rules live in `~/.codex/rules/`, which the installer would replace, so none ship.
- Models: the user picks the pair per thread (GPT-6 Astra, Fable 5.1, Opus 5.5). Implement and review are Opus 5.5 / gpt-6-sol at high; general is Opus 5.5 / gpt-6-sol at medium; explore is Opus 5.5 at low / gpt-6-luna at high; git is Opus 5.5 at low / gpt-6-luna at medium; browser is Opus 5.5 at low / gpt-6-sol at medium.

## Settled decisions

Do not reopen these without contradictory current evidence:

| Decision                                                                                                                              | Why                                                                                                                                                                                                                                   |
| ------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| One pair thread, coordinator only                                                                                                     | Inline Bash was 70–82% of Fable main-thread spend; the pair ran alone for 61–98% of busy time                                                                                                                                         |
| Two phases for substantial work: shape, then execute                                                                                  | 62% of 240 user prompts were steering; shaping moves decisions first, execution runs without check-ins                                                                                                                                |
| Acceptance checks chosen per task during shaping                                                                                      | The user was the only acceptance test: 15 prompts reported defects the agent could have verified                                                                                                                                      |
| Simplicity section leads the engineering skill                                                                                        | 22 overbuilt incidents: abstractions, options, docs, checks, leftovers, unrelated edits                                                                                                                                               |
| Checks, lint, reviewers, and the user are fallbacks                                                                                   | Codemods and implement runs iterated through type errors instead of designing first                                                                                                                                                   |
| Implement at high effort; one adversarial review per delivery                                                                         | The old review role ran on every change for up to 10 fix rounds; one fresh pass at the end found about 30% removable code in 2.5 minutes                                                                                              |
| Mechanics automatic; the pair waits once on the pipeline after reporting the request                                                  | "commit", "push", "update the PR" were requested more than 12 times; a git run's 19-minute pipeline wait hid a finished request                                                                                                       |
| Never commit, push, or merge into the default branch                                                                                  | Two direct pushes to main; the only merge brings another branch into the feature branch                                                                                                                                               |
| A decision ledger checks merges and wide changes                                                                                      | An align-with-master checkout restored deliberately deleted files                                                                                                                                                                     |
| Repository instructions never widen the request                                                                                       | Repository-mandated extras become listed follow-ups                                                                                                                                                                                   |
| Schematic output; context explained, no assumed codebase knowledge                                                                    | User rejected prose-heavy and assumption-heavy reports                                                                                                                                                                                |
| No custom hooks or scripts; harness configuration only                                                                                | Hook scripts and generators were built and removed three times                                                                                                                                                                        |
| Mirrored source files, no generator; project skills by symlink                                                                        | Two copied bodies are simpler than rendering; a symlink keeps one skill source for both harnesses                                                                                                                                     |
| Launch independent owners together; reuse valid proof                                                                                 | Serial acquisition and repeated checks were recurring latency costs                                                                                                                                                                   |
| Stop a prototype on its first deciding observation                                                                                    | Claude expanded a failed cheap mechanism into a scanner and timed out                                                                                                                                                                 |
| Facts, possible failures, and runtime claims are distinct                                                                             | Reviews conflated prompt intent, hook behavior, and runtime guarantees                                                                                                                                                                |
| Memory stays disabled                                                                                                                 | User decision                                                                                                                                                                                                                         |
| Goals stay off                                                                                                                        | Current models finish without them; goals caused loops                                                                                                                                                                                |
| No per-repository knowledge skills                                                                                                    | Grounding reads the nearest AGENTS.md and the code that enforces each contract                                                                                                                                                        |
| Pair tuned to how the user writes                                                                                                     | 249 Claude and 857 Codex prompts: bundled asks, hypotheses marked with idk or probably, why for root causes, top-to-bottom reading                                                                                                    |
| Quotas: Claude 20x, Codex 5x, used in parallel all week                                                                               | The scarcest models go only where they remove rework                                                                                                                                                                                  |
| Mirror the nearest sibling; engineering skill over repo standards                                                                     | 3 of 5 commits in a dual MR fixed a first slice that ignored its sibling's permission, helper, invalidation, and test style                                                                                                           |
| Align with the target before the final gate; "align" on demand                                                                        | A dual MR was out of date at "done"; conflicts stay user decisions                                                                                                                                                                    |
| One delivery: commit, push, attached screenshots, body                                                                                | 6 git runs and separate screenshot passes; `gh`/`glab --attach` replace API uploads                                                                                                                                                   |
| Browser asserts by snapshot; screenshots only for the request                                                                         | One retake run read about 30 PNGs back and used 19.3M of the thread's 77M tokens                                                                                                                                                      |
| Unrelated problems are proposals, only in the reply                                                                                   | The user rejected follow-ups in the request body and fixes outside scope                                                                                                                                                              |
| Default-branch protection stays prompt- and deny-level                                                                                | User decision; no server-side rulesets                                                                                                                                                                                                |
| Commands end on their own; a hang is a blocker, never polled or waited out                                                            | Default Bash timeout cut from 60 to 10 minutes: a hung `bunx shadcn add` blocked a slice 82 minutes and a poll waited 25 minutes on another agent's install                                                                           |
| Browser runs once per delivery through `vpx agent-browser`, one login                                                                 | An 84-minute `npx --yes agent-browser` run had a 40 s median call against 0.4 s with `bunx`; warm overhead per call: npx 5.6 s, vpx 1–1.7 s, bunx 0.3 s                                                                               |
| Prototypes during shaping without asking, no checks, deleted at the pick                                                              | User decision; five checked prototype runs with fix loops used about 50M tokens                                                                                                                                                       |
| Expensive steps once per delivery; proof scales with the change                                                                       | One iteration spent 14 minutes writing code and over 2 hours on browser, tests, check, and pipeline; a few-line follow-up gets narrow proof and delivery only                                                                         |
| `vp` and `vpx` only, in every repository and role                                                                                     | User decision, no exceptions                                                                                                                                                                                                          |
| Environment launches with grounding; repeated operations are piloted once                                                             | Every thread starts in a fresh worktree; one agent waited 25 minutes on another's install, and a slice repeated a 45-minute `shadcn add` for 22 more logos                                                                            |
| Opus 5.5 at low effort replaces Sonnet 5 for explore, git, and browser                                                                | CursorBench 4.0: Opus 5.5 low 43.7% at $1.17, 15.8k tokens, 28 steps against Sonnet 5 medium 28.0% at $2.31, 39k tokens, 65 steps; Opus 5.5 high equals xhigh at 57% of the cost; Fable 5.1 costs more and scores lower than Opus 5.5 |
| Review raises every plan, contract, or skill violation, however small                                                                 | A gpt-6-sol review answered "No merge-blocking findings" with 2 planted leftovers left                                                                                                                                                |
| Plain code: no comments, no indirection one use does not need, no cleverness                                                          | User decision                                                                                                                                                                                                                         |
| Status answers read the agent's latest command from its transcript and claim nothing more                                             | A stat of the symlinked transcript answered "it isn't stuck" while `gen` hung                                                                                                                                                         |
| Vendor prompting guidance: positive rules, good examples first, parallel requests, a time budget, no second self-check, one long wait | Anthropic guides for Opus 5.5, Sonnet 5, and Fable 5.1; OpenAI GPT-6 Astra guide and Codex model docs; a Codex pair spent 1.02M tokens over 28 short waits                                                                            |
| One acceptance check runs the user's scenario end to end for every change                                                             | Replays: both agents' lint fixes passed their own tests yet broke the skill's canonical recursive example                                                                                                                             |
| A definition the request leaves open makes work substantial                                                                           | The recursive-schema replay was treated as a bug fix and both agents chose a syntactic proxy                                                                                                                                          |
| The final gate is the full local check with tests                                                                                     | A dual replay ran fmt, lint, and types only and shipped a broken help snapshot                                                                                                                                                        |
| Setup is always in the first dispatch; installs and builds get the maximum timeout                                                    | A fresh dual install took 11 minutes inside a slice and hit the 10-minute default                                                                                                                                                     |
| Codex waits are one `wait_agent` call with `timeout_ms: 3600000`                                                                      | A Codex pair spent 17.5M tokens over 71 waits of 10–60 s                                                                                                                                                                              |
| git uses the configured identity and writes no config                                                                                 | A Codex git agent copied an author email from `git log` into the clone's config                                                                                                                                                       |

## Evidence index

Use these artifacts instead of reconstructing prior investigations:

| Evidence                            | Location                                                                                                        | Decisive result                                                                                                                                                                                                                                                                                                                                                        |
| ----------------------------------- | --------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Short-contract comparison           | /home/mp281x/.deslop/measure/short-contract-eval/                                                               | Identical manifests, 12 runs. Codex review deadline to 51.351s, prototype 76.346 to 57.562s, implementation 40.191 to 27.189s. Claude review 79.654 to 55.027s, prototype 76.388 to 91.052s deadline with scanner and no final, implementation 17.074 to 15.352s. All implementation external tests green with one source edit.                                        |
| Source-review runs                  | /home/mp281x/.deslop/measure/workflow-cli-review/                                                               | Codex grounded but loaded full engineering and repeated discovery. Claude serialized calls, conflated hooks, and expanded into speculative configuration. Scratch-home adjacency limits installed-state conclusions.                                                                                                                                                   |
| Prototype demonstration             | /home/mp281x/.deslop/measure/prototype-first-demo/                                                              | Initial Codex asked permission; initial Claude built a scanner and timed out. Later Codex ran a bounded probe; later Claude stopped expanding, but overexplained and invented estimates.                                                                                                                                                                               |
| Prompt procedure eval               | /home/mp281x/.deslop/measure/prompt-procedure-eval/                                                             | Narrow bug fixes succeeded but were slower after; design requests still invented scale and overdesigned. Installer scratch smoke preserved personal skills.                                                                                                                                                                                                            |
| Coordination eval                   | /home/mp281x/.deslop/measure/workflow-quick-eval/                                                               | Codex native waits and follow-up worked. Claude needed SendMessage; one run spawned an unnecessary waiting agent.                                                                                                                                                                                                                                                      |
| Manual regression cases             | [recurring corrections](references/recurring-corrections.md)                                                    | Completion, evidence reuse, batching, design bounds, prototype stopping, and factual-review boundaries.                                                                                                                                                                                                                                                                |
| 2026-09-22 thread analysis          | ~/.claude/projects/-home-mp281x--t3-worktrees-deslop-t3code-05561f1c/47dde977-6225-46e8-ba87-eb9ca3b79743.jsonl | 17 threads, 240 prompts classified: 62% steering; 22 overbuilt incidents; parallelism, token spend, and harness capabilities measured                                                                                                                                                                                                                                  |
| 2026-09-23 dual thread              | ~/.claude/projects/-home-mp281x--t3-worktrees-dual-t3code-826b875c/e6375478-a22d-4f16-a83b-f6ebf2170533.jsonl   | Opus 5.5 pair, deploy Run button: 77M input tokens, browser 33%; 3 churn commits from missed sibling contracts; pair ran 50+ Bash calls; credentials rediscovered by browser                                                                                                                                                                                           |
| 2026-09-23 evals                    | ~/.claude/projects/-tmp-eval-batch/, ~/.claude/projects/-tmp-eval-review/                                       | Opus 5.5 pair on scratch clones: three check defects fixed in 132 s with 1 brief, 2 repo checks, 1 commit; review-then-fix in 314 s with 1 repo check, 2 kept deletions, speculative changes rejected. Small tasks behave; remaining failures were multi-hour, multi-slice dual tasks                                                                                  |
| 2026-09-24 dual model-picker thread | ~/.claude/projects/-home-mp281x--t3-worktrees-dual-t3code-b58f7917/01e1831a-09e7-464b-98df-f7c260cf2f48.jsonl   | Opus 5.5 pair, 3 prompts over 6 h; subagents 176M tokens, browser 103M; npx browser run 40 s median call; shadcn hang 82 min; pipeline wait 19 min in the git run; pair killed a process itself; about 30% of the diff removable                                                                                                                                       |
| 2026-09-24 evals                    | ~/.claude/projects/-tmp-eval-hang/, -tmp-eval-done/, -tmp-eval-proto/                                           | Installed version, Opus 5.5 pair: a hung generator reported as a blocker in 160 s with no polling; finishing a branch with 5 planted leftovers ran review once (5 findings), one fix round, pushed in 172 s; a rename follow-up took 40 s with narrow proof only; a vague design request got 3 parallel prototypes in 80 s with no checks; Codex loads the review role |
| 2026-09-24 evals, round 2           | ~/.claude/projects/-tmp-eval-web/, -tmp-eval-status/; ~/.codex/sessions/2026/09/24/                             | Claude rendered app end to end in 5m59s: one 147 s browser run via vpx with 0 screenshot reads, review once, one fix round; wrong status answer from a symlink stat; Codex finish-branch in 514 s with 2 leftovers missed by review and 1.02M pair tokens                                                                                                              |
| 2026-09-24 model research           | cursor.com/cursorbench, artificialanalysis.ai, vendor prompting guides                                          | Opus 5.5 beats Sonnet 5 at every effort in fewer steps; no per-effort coding-agent data for gpt-6-sol or gpt-6-luna                                                                                                                                                                                                                                                    |
| 2026-09-24 replays                  | /tmp/replay/results/, ~/.claude/projects/-tmp-replay-*, ~/.codex/sessions/2026/09/24/                           | Blind judge: deslop lint fix scored merged 6.5, Claude 4, Codex 2.5; dual cleanup scored Codex 6, merged 4.5, Claude cut off by a headless 600 s limit; deslop took 17 min on Claude and 25 min on Codex                                                                                                                                                               |
| 2026-09-24 thread analyses          | Codex deslop/t3code-a3a163af and dual/t3code-b58f7917; Claude 01e1831a and e474c368 new turns                   | Codex waits dominated pair tokens; Python used 43 times across agents; a merge commit took three agent runs; a long-lived thread repeated its own earlier self-execution when told to follow the same steps                                                                                                                                                            |

Do not compare elapsed times across changed prompts, harness versions, fixtures, hosts, or isolation. Aggregate input includes cached tokens and is not unique context size.

## Package and engineering facts retained

- tools/workflow/src/agents/skills/engineering/SKILL.md is one file of executable good and bad product-code conventions. project-engineering holds repository-specific layout and service rules.
- The workflow package owns its agent assets, installer, shared Oxlint config, ten repository rules, and exported TypeScript config.
- Published TypeScript is built because Node 26 refuses type stripping under node_modules.
- Consumers restate the ignorePatterns and rule options they want; Oxlint merges overrides across extends but replaces those fields.
- A jsPlugins specifier resolves from the linted root. Formatter configuration is duplicated per root because oxfmt has no extends.
- Releases publish @deslop/workflow from main as 0.1.<run number>. The external consumer probe is ~/.deslop/harness-probe/turbo-probe.
